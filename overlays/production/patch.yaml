apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: httpd-example
spec:
  template:
    spec:
      volumes:
      - name: drtest
        persistentVolumeClaim:
          claimName: drtest2
